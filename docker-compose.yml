version: '3.8'
services:
  miner:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install && node mine.js"
    restart: unless-stopped
    env_file:
      - .env 